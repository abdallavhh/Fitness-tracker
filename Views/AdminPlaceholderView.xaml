<UserControl x:Class="FitnessTracker.Views.AdminPlaceholderView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             Background="{StaticResource Ft.Brush.Page}"
             FontFamily="{StaticResource Ft.Font.App}">

    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Padding="0">
        <Border Style="{StaticResource Ft.Card}" VerticalAlignment="Center" HorizontalAlignment="Stretch" MaxWidth="560"
                Margin="40,30,40,30" Padding="28,28">
            <StackPanel>
                <Border Width="52" Height="52" CornerRadius="14" Background="{StaticResource Ft.Brush.PrimarySoft}" HorizontalAlignment="Left" Margin="0,0,0,16">
                    <materialDesign:PackIcon Kind="FileDocumentOutline" Width="28" Height="28" Foreground="{StaticResource Ft.Brush.Primary}"
                                             HorizontalAlignment="Center" VerticalAlignment="Center"/>
                </Border>
                <TextBlock Text="{Binding Title}" FontSize="24" FontWeight="Bold" Foreground="{StaticResource Ft.Brush.Heading}" Margin="0,0,0,10" TextWrapping="Wrap"/>
                <TextBlock Text="{Binding Message}" FontSize="15" LineHeight="22" Foreground="{StaticResource Ft.Brush.Label}" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>
    </ScrollViewer>
</UserControl>

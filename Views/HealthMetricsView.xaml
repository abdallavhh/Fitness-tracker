<UserControl x:Class="FitnessTracker.Views.HealthMetricsView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:vm="clr-namespace:FitnessTracker.ViewModels"
             xmlns:converters="clr-namespace:FitnessTracker.Converters"
             Background="{StaticResource Ft.Brush.Page}"
             FontFamily="{StaticResource Ft.Font.App}">

    <UserControl.Resources>
        <converters:HexToBrushConverter x:Key="HexToBrush"/>
    </UserControl.Resources>

    <Grid Margin="40,30,40,30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0" Style="{StaticResource CardStyle}" Margin="0,0,0,16" Padding="24">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*"/>
                    <ColumnDefinition Width="Auto"/>
                </Grid.ColumnDefinitions>
                <TextBlock Grid.Column="0"
                           Text="Health Metrics"
                           FontSize="26"
                           FontWeight="Bold"
                           Foreground="{StaticResource Ft.Brush.Heading}"
                           TextWrapping="Wrap"
                           VerticalAlignment="Center"/>
                <Button Grid.Column="1"
                        Content="+ Add Metric"
                        Style="{StaticResource App.Styles.Button.Primary}"
                        Command="{Binding AddMetricCommand}"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center"
                        Margin="16,0,0,0"/>
            </Grid>
        </Border>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto">
            <ItemsControl ItemsSource="{Binding Metrics}">
                <ItemsControl.ItemsPanel>
                    <ItemsPanelTemplate>
                        <WrapPanel Orientation="Horizontal"/>
                    </ItemsPanelTemplate>
                </ItemsControl.ItemsPanel>
                <ItemsControl.ItemTemplate>
                    <DataTemplate DataType="{x:Type vm:HealthMetricItemVm}">
                        <Border MinWidth="200" MaxWidth="360" Margin="0,0,12,12" CornerRadius="8" Padding="16"
                                Background="{Binding TileBackgroundHex, Converter={StaticResource HexToBrush}}"
                                Effect="{StaticResource SoftShadow.Effect.ListItem}">
                            <Grid>
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="Auto"/>
                                    <ColumnDefinition Width="*"/>
                                </Grid.ColumnDefinitions>
                                <materialDesign:PackIcon Grid.Column="0" Kind="{Binding IconKind}" Width="32" Height="32"
                                                         VerticalAlignment="Top" Margin="0,0,14,0"
                                                         Foreground="{Binding IconColorHex, Converter={StaticResource HexToBrush}}"/>
                                <StackPanel Grid.Column="1">
                                    <TextBlock Text="{Binding Name}" FontSize="13" FontWeight="SemiBold" Foreground="{StaticResource Ft.Brush.Label}"/>
                                    <TextBlock Text="{Binding Value}" FontSize="20" FontWeight="Bold" Margin="0,8,0,0"
                                               Foreground="{Binding ValueColorHex, Converter={StaticResource HexToBrush}}"/>
                                </StackPanel>
                            </Grid>
                        </Border>
                    </DataTemplate>
                </ItemsControl.ItemTemplate>
            </ItemsControl>
        </ScrollViewer>
    </Grid>
</UserControl>
